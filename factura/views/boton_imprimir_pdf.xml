<?xml version="1.0" encoding="utf-8"?>
<odoo>

    
    <record id="boton_sale_order" model="ir.actions.report">
        <field name="name">Factura</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">factura.reporte_factura_personalizado</field>
        <field name="report_file">factura.reporte_factura_personalizado</field>
        <field name="print_report_name">'Orden de Trabajo - %s' % (object.name)</field>
        <field name="paperformat_id" ref="factura.paperformat_letter_size"/>
        <field name="binding_model_id" ref="model_sale_order"/>
    </record>

</odoo>
