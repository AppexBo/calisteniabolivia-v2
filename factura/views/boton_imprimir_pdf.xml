<?xml version="1.0" encoding="utf-8"?>
<odoo>

    
    <record id="boton_sale_order" model="ir.actions.report">
        <field name="name">Orden de Trabajo</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">factura.cuerpo_pdf</field>
        <field name="report_file">factura.cuerpo_pdf</field>
        <field name="print_report_name">'Orden de Trabajo - %s' % (object.name)</field>
        <field name="paperformat_id" ref="factura.formato_papel_sale_order"/>
        <field name="binding_model_id" ref="model_sale_order"/>
    </record>

</odoo>
